# CV knowledge for Interactive CV

Markdown files in this folder (except `README.md`) are loaded by the serverless API at `/api/cv-chat` and sent to the model as context. Edit these files to change what the assistant can say.
